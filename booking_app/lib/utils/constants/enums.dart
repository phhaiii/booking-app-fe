enum TextSizes { small, medium, large }
enum NotificationType { message, appointment }
enum AppointmentStatus { 
  confirmed,    // Đã xác nhận
  pending,      // Chờ xác nhận
  cancelled,    // Đã hủy
  completed,    // Đã hoàn thành
  rescheduled   // Đã đổi lịch
}
